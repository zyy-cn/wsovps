You are being given an already-prepared gate-overlay package for WSOVPS.

Your job in this round is DEPLOYMENT + NORMALIZATION + DESIGN-INTAKE ONLY.

Do NOT start implementation.
Do NOT start training.
Do NOT start benchmarking.
Do NOT widen scope into E5 or S3 execution.
Do NOT reinterpret the scientific roadmap beyond the supplied overlay.

Primary goals:
1. Deploy the overlay into the project root.
2. Normalize the mainline control plane so that the post-S2 next scientific target becomes S2A, with E4A as the minimum supporting engineering gate.
3. Preserve all settled S2 PASS evidence and provenance.
4. Refresh local canonical surfaces, especially TAKEOVER, so the next web-side review can proceed from local truth only.

======================================================================
READ ORDER
======================================================================

1. Run:
   `python tools/validate_state_views.py`

2. If validation is OK, read:
   - `docs/mainline/CURRENT_LOOP_BRIEF.md`
   - `docs/mainline/CURRENT_GATE_PACK.md`
   - `docs/mainline/CURRENT_EXECUTION_TICKET.md`
   - `docs/mainline/takeover/TAKEOVER_LATEST.md`

3. If validation is STALE or CONFLICTED, fall back to canonical docs under `docs/mainline/*`, especially:
   - `AGENTS.md`
   - `docs/mainline/OPERATING_CONSTITUTION.md`
   - `docs/mainline/DEVELOPMENT_GATE_CONTROL.md`
   - `docs/mainline/EXPERIMENT_MANAGEMENT_POLICY.md`
   - `docs/mainline/DESIGN_PACK_PROTOCOL.md`
   - `docs/mainline/ENVIRONMENT_AND_VALIDATION.md`

4. Read the newly deployed overlay content, especially:
   - `docs/mainline/gates/REGISTRY.json`
   - `docs/mainline/gates/engineering/E4A_ORACLEOBJ_LOSS_ATTRIBUTION_INSTRUMENTATION.md`
   - `docs/mainline/gates/scientific/S2A_ORACLEOBJ_LOSS_MECHANISM_VALIDATION.md`
   - `docs/mainline/designs/GATE_E4A/DP-E4A-ORACLEOBJ-LOSS-ATTRIBUTION-INSTRUMENTATION-R1/DESIGN_MANIFEST.json`
   - `docs/mainline/designs/GATE_S2A/DP-S2A-ORACLEOBJ-LOSS-MECHANISM-VALIDATION-R1/DESIGN_MANIFEST.json`
   - `docs/mainline/experiments/REGISTRY.json`

======================================================================
DEPLOYMENT TASK
======================================================================

Apply the overlay to the LOCAL project root first.

Then perform only the minimum normalization needed so the control plane reflects:
- S2 is already PASS and preserved
- E4A is the next active/supporting engineering gate
- S2A is the next scientific target
- E5/S3 remain downstream and not yet active

======================================================================
REQUIRED NORMALIZATION
======================================================================

1. Preserve S2 PASS truth and existing evidence.
2. Normalize gate-chain truth so the mainline chain becomes:
   `E2 -> E3 -> E4 -> S2 -> E4A -> S2A -> E5 -> S3 -> E6 -> S4 -> E7 -> S5`
3. Normalize experiment-registry truth so new prepared placeholders exist for:
   - `EXP-E4A-ORACLEOBJ-LOSS-ATTRIBUTION-INSTRUMENTATION`
   - `EXP-S2A-ORACLEOBJ-LOSS-MECHANISM-VALIDATION`
4. Use project tooling to refresh rendered surfaces where required.
5. Create a new current execution ticket for E4A/S2A DESIGN-INTAKE ONLY.

The new current execution ticket must make all of the following explicit:
- active_gate / main target is post-S2 insertion work, not renewed S2 repair
- active_scientific_gate: `S2A`
- supporting_engineering_gates: `['E4A']`
- delivery_mode: `design_pack`
- bound design packs:
  - `DP-E4A-ORACLEOBJ-LOSS-ATTRIBUTION-INSTRUMENTATION-R1`
  - `DP-S2A-ORACLEOBJ-LOSS-MECHANISM-VALIDATION-R1`
- this round is design/intake only
- no implementation, no training, no benchmark execution

======================================================================
NOT ALLOWED THIS ROUND
======================================================================

- no model-code implementation
- no E4A instrumentation landing
- no S2A experiment execution
- no E5/S3 execution
- no gate promotion without normalized canonical surfaces

======================================================================
REQUIRED OUTPUTS
======================================================================

Refresh local canonical surfaces so they are coherent after overlay deployment:
- `docs/mainline/STATUS.md`
- `docs/mainline/CURRENT_EXECUTION_TICKET.md`
- `docs/mainline/state/CURRENT_EXECUTION_TICKET.json`
- `docs/mainline/experiments/ACTIVE_EXPERIMENTS.md`
- any tool-rendered gate / experiment indexes that should now include E4A / S2A
- local `docs/mainline/takeover/TAKEOVER_LATEST.md`
- local `docs/mainline/takeover/TAKEOVER_LATEST.json`

Also write:
- `docs/mainline/reports/e4a_s2a_gate_overlay_deployment_latest.md`

That deployment report must state:
- what overlay files were applied
- whether validators passed
- what canonical surfaces were normalized
- whether any stale/conflicting source remained
- explicit confirmation that no implementation or experiment execution occurred

======================================================================
STOP CONDITION
======================================================================

Stop after:
- overlay is deployed
- control-plane normalization is complete
- local takeover is refreshed
- the deployment report is written

If any conflict remains between the overlay and current canonical docs, report the exact conflict and stop.
