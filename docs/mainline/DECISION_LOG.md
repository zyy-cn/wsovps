# Decision Log

## 2026-03-28 — gate-pack-driven private-layer adoption
- Accepted that the uploaded new gate pack supersedes outline-driven planning for the running WSOVPS mainline.
- Preserved the approved active chain as `E2 -> E3 -> E4 -> S2 -> E5 -> S3 -> E6 -> S4 -> E7 -> S5`.
- Preserved `E0`, `E1`, and `S1` as archived historical prerequisites only.
- Installed a tool-compatible normalized gate registry because the shipped gate pack registry shape is not directly compatible with the current validators.
- Chose not to fabricate detailed historical experiment evidence that was absent from the upload; historical prerequisite status is recorded as archived context and must be re-linked during E2 if source artifacts exist in the deployed repo.
- Set the first active execution scope to E2 settlement, not E3 or S2.
